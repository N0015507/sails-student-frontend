<ol class="breadcrumb">
    <li><a href="/">Home</a></li>
    <li class="active">Delete Employee</a></li>
</ol>

<br/>

<%- partial ('partials/statusmessage.ejs') %>

<h2>Delete Student</h2>

<div style="text-align: center; padding-top:50px">
  <form action="/delete" method="POST" id="deleteStudentForm">
    <div class="form-group">
      <label for="student_id">SELECT A STUDENT TO DELETE</label><br/>
      <select id="student_id" name="student_id">
      <% if(typeof students != "undefined" && students.length > 0){ %>
        <% students.forEach(function(student){ %>
          <option value="<%= student.student_id %>" data-icon="glyphicon-user">
            <%= student.last_name + ", " +  student.first_name %>
          </option>
        <% }) %>
      <% } %>
      </select>
    </div>
    <div class="form-group">
      <button id="delBtn" type="submit" class="btn btn-danger">
        DELETE STUDENT
        <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
      </button>
    </div>
  </form>
</div>
